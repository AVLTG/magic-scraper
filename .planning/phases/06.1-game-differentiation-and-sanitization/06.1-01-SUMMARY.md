---
phase: 06.1-game-differentiation-and-sanitization
plan: 01
plan_name: import-flag-backfill
status: complete
completed: 2026-04-11
tasks_completed: 4
tasks_total: 4
commits:
  - 0a44fa4: feat(06.1-01) add isImported flag to Game model
  - ca7a481: docs(06.1-01) add production migration SQL for isImported backfill
  - 1ba76da: docs(schema) document isImported column and Phase 7 semantics
key_files:
  created:
    - .planning/phases/06.1-game-differentiation-and-sanitization/06.1-01-PROD-MIGRATION.sql
  modified:
    - prisma/schema.prisma
    - .planning/codebase/SCHEMA.md
  database_mutations:
    - local_dev: "prisma db push applied Game.isImported column"
    - production: "turso db shell (manual) applied ALTER TABLE + UPDATE backfill — imported_rows = 20"
---

# Plan 06.1-01: Import Flag Backfill — SUMMARY

## Outcome

Added `isImported Boolean @default(false)` to the `Game` Prisma model, pushed the
schema change to local dev via `prisma db push`, generated a reviewable
production migration SQL script, had the human operator apply it to production
Turso via `turso db shell`, and documented the new column + its Phase 7
semantic role in `.planning/codebase/SCHEMA.md`.

## Task Execution

### Task 1: Add `isImported` to Game model + push to local dev DB — `0a44fa4`
- Inserted `isImported Boolean @default(false)` between `notes` and `createdAt`
  in `prisma/schema.prisma` (Game model only — NOT GameParticipant, per D-01)
- Ran `npx prisma db push` against local dev Turso — additive only, no
  data-loss prompt
- Ran `npx prisma generate` — client regenerated with the new field on the
  Game type
- `npx tsc --noEmit` exits 0
- `npx jest` full suite remained green (97/97 tests)
- PowerShell spot-check via `prisma db execute --file` confirmed the column
  exists in local dev

### Task 2: Generate production migration SQL — `ca7a481`
- Created `.planning/phases/06.1-game-differentiation-and-sanitization/06.1-01-PROD-MIGRATION.sql`
  (31 lines) with:
  - `ALTER TABLE games ADD COLUMN isImported INTEGER NOT NULL DEFAULT 0`
    (SQLite maps Prisma Boolean → INTEGER 0/1)
  - `pragma_table_info` verification SELECT
  - Total/default-count verification SELECT
  - `UPDATE games SET isImported = 1 WHERE notes LIKE '%Ported from Spreadsheet%'`
  - `imported_rows` count verification SELECT
  - Notes-preservation spot-check SELECT (D-04)
- Additive-only per Phase 5 D-16 — no DROP, no DELETE, no transaction wrapper
- No index on `isImported` — premature for ~hundreds of rows in aggregate scans

### Task 3: Apply prod ALTER + backfill via `turso db shell` — HUMAN ACTION (no commit)
- Human operator ran `turso db shell magic-scraper` interactively against
  `libsql://magic-scraper-avltg.aws-us-east-2.turso.io`
- Pasted statements from the migration SQL one at a time:
  1. `ALTER TABLE` — no output, no error
  2. `pragma_table_info` — confirmed `isImported | INTEGER | 1 | 0`
  3. Total/default count — `total_games = 20`, `default_false = 20`
  4. `UPDATE` — applied
  5. `imported_rows` count — **20** (exactly matches D-03 target; inside
     the plan's [1, 30] acceptance bound)
  6. Spot-check — all 3 sampled rows still contain the literal substring
     `Ported from Spreadsheet` in their `notes` column (D-04 preserved)

### Task 4: Document `isImported` in `.planning/codebase/SCHEMA.md` — `1ba76da`
- Added a new row for `isImported` between `notes` and `createdAt` in the
  `games (Prisma model Game)` table
- Added a "Phase 6.1 addendum — `isImported`" paragraph block immediately
  after the games table section, covering:
  - Backfill path (prisma db push + turso db shell ALTER)
  - D-04: notes field intentionally NOT cleaned up
  - Phase 7 semantics: combo/deck metrics MUST filter `WHERE isImported = false`
    (D-06); player-rate / screwed-rate / weekly frequency include imported
    rows (D-07)
  - UI invariant (D-02): no file under `src/app/games/**` or
    `src/app/api/games/**` may read, write, or display this field

## Acceptance Criteria — Status

All plan-level `<success_criteria>` met:

- [x] `Game.isImported` field exists in schema, local dev DB, and production DB
- [x] Production 20 legacy rows flagged (verified via SELECT COUNT)
- [x] No UI file under `src/app/games/**` or `src/app/api/games/**` modified
      (D-02 hidden-flag invariant preserved)
- [x] Phase 7 can now filter stats via `WHERE isImported = false`
- [x] SCHEMA.md documents the column and its semantics

## Key Decisions Honored

- **D-01**: `isImported Boolean @default(false)` on Game model only, not
  GameParticipant
- **D-02**: Hidden flag — zero UI/API surface
- **D-03**: Backfill via SQL `UPDATE ... WHERE notes LIKE '%Ported from Spreadsheet%'`
  — 20 rows flagged (exactly matches target; user had said "20 is not that crazy")
- **D-04**: Notes field preserved literally — two redundant indicators accepted
- **D-05**: `prisma db push` for dev, `turso db shell` for prod
  (Phase 5 D-13/D-14 pattern — `prisma migrate deploy` is Turso-incompatible)
- **D-06 / D-07**: Phase 7 semantics documented in SCHEMA.md addendum

## Deviations

**None from the plan's intent.** Minor procedural note: the human operator
initially tried to redirect the SQL file into `turso db shell` from the main
checkout (`/mnt/d/homework/coding/magic-scraper`), where the file doesn't
exist because it was committed on the isolated worktree branch
`worktree-agent-a34cfe18`. Switched to the interactive-paste path instead —
same SQL statements, same verification outputs. No scope or decision change.

## Downstream Impact

- **Plan 06.1-02** (duplicate player prevention) and **Plan 06.1-03** (games
  filter toolbar) both explicitly do NOT touch `isImported` (D-02 invariant).
- **Phase 7** (stats dashboard) can now gate combo-rate and per-deck metrics
  with `WHERE isImported = false`.
- **Phase 8** (admin sync) — no interaction; sync_logs is a different table.
